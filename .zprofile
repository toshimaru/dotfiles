# Enable homebrew
eval "$(/opt/homebrew/bin/brew shellenv)"

# Enable mise (unless you use oh-my-zsh plugin)
# eval "$(mise activate zsh)"

# Enable starship
# if you are an oh-my-zsh user, write this in your `.zshrc` file after initializing oh-my-zsh
# eval "$(starship init zsh)"
